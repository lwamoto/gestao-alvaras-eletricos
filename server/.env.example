MONGODB_URI=mongodb+srv://<usuario>:<senha>@<cluster>.mongodb.net/alvaras?retryWrites=true&w=majority
PORT=4000

# Gere um valor forte, ex: openssl rand -hex 32 — nunca commite o valor real
JWT_SECRET=<segredo-aleatorio-forte>
CLIENT_ORIGIN=http://localhost:5173
